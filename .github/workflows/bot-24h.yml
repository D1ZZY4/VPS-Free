name: Bot 24 Hours With Auto Backup

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  bot-runner:
    runs-on: ubuntu-latest
    timeout-minutes: 1440

    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: admin@123
          POSTGRES_DB: telebot
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc python3-dev postgresql-client curl jq

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Wait for PostgreSQL
        run: |
          until pg_isready -h localhost -p 5432 -U postgres; do
            sleep 3
          done

      - name: Create database if not exists
        env:
          PGPASSWORD: admin@123
        run: |
          psql -h localhost -U postgres -c "CREATE DATABASE telebot;" || true

      # =========================
      # RESTORE FROM RELEASE
      # =========================
      - name: Restore db.sql & config.ini from GitHub Release (if exists)
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          PGPASSWORD: admin@123
        run: |
          set +e
          API_URL="https://api.github.com/repos/${GITHUB_REPOSITORY}/releases/tags/autosave"

          RESPONSE=$(curl -s -H "Authorization: token $GH_TOKEN" $API_URL)
          ASSET_URL_DB=$(echo "$RESPONSE" | jq -r '.assets[] | select(.name=="db.sql") | .url')
          ASSET_URL_CFG=$(echo "$RESPONSE" | jq -r '.assets[] | select(.name=="config.ini") | .url')

          if [ "$ASSET_URL_DB" != "null" ]; then
            echo "⬇️ Restoring db.sql from release"
            curl -L -H "Authorization: token $GH_TOKEN" -H "Accept: application/octet-stream" "$ASSET_URL_DB" -o db.sql
            psql -h localhost -U postgres telebot < db.sql
          else
            echo "ℹ️ No db.sql found, starting fresh"
          fi

          if [ "$ASSET_URL_CFG" != "null" ]; then
            echo "⬇️ Restoring config.ini from release"
            curl -L -H "Authorization: token $GH_TOKEN" -H "Accept: application/octet-stream" "$ASSET_URL_CFG" -o config.ini
          fi

      # =========================
      # MAIN LOOP
      # =========================
      - name: Run bot with 2.5h backup cycle
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          OWNER_ID: ${{ secrets.OWNER_ID }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          PGPASSWORD: admin@123
        run: |
          #!/bin/bash
          set -e

          INTERVAL=9000  # 2.5 jam
          RELEASE_TAG="autosave"

          while true; do
            echo "▶ Starting bot"
            python -m tg_bot &
            BOT_PID=$!

            echo "⏳ Bot running for 2.5 hours"
            sleep $INTERVAL

            echo "⛔ Stopping bot"
            kill $BOT_PID
            wait $BOT_PID 2>/dev/null || true

            TS=$(date +"%Y%m%d_%H%M%S")

            echo "🗄️ Dump database"
            pg_dump -h localhost -U postgres telebot > db.sql

            echo "📤 Upload db.sql & config.ini to GitHub Release"

            RELEASE_JSON=$(curl -s -H "Authorization: token $GH_TOKEN" \
              https://api.github.com/repos/${GITHUB_REPOSITORY}/releases/tags/$RELEASE_TAG)

            RELEASE_ID=$(echo "$RELEASE_JSON" | jq -r '.id')

            if [ "$RELEASE_ID" = "null" ]; then
              RELEASE_ID=$(curl -s -X POST -H "Authorization: token $GH_TOKEN" \
                -H "Content-Type: application/json" \
                -d "{\"tag_name\":\"$RELEASE_TAG\",\"name\":\"Auto Backup\",\"body\":\"Bot autosave\"}" \
                https://api.github.com/repos/${GITHUB_REPOSITORY}/releases | jq -r '.id')
            fi

            for FILE in db.sql config.ini; do
              if [ -f "$FILE" ]; then
                curl -s -X POST -H "Authorization: token $GH_TOKEN" \
                  -H "Content-Type: application/octet-stream" \
                  --data-binary @"$FILE" \
                  "https://uploads.github.com/repos/${GITHUB_REPOSITORY}/releases/${RELEASE_ID}/assets?name=$FILE" || true
              fi
            done

            echo "📦 Send logs to owner"
            tar -czf logs_$TS.tar.gz logs.txt *.log 2>/dev/null || true

            curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendDocument" \
              -F chat_id="${OWNER_ID}" \
              -F document=@logs_$TS.tar.gz \
              -F caption="🧾 Logs\n⏱ $TS"

            rm -f logs_$TS.tar.gz
          done